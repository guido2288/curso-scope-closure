function greeting() {
  let userName = 'Guido'; // asignamos y declaramos la variable;
  console.log(userName);

  if (userName === 'Guido') {
    console.log(`Hello ${userName}!`);
  }
};

greeting();
console.log(userName);